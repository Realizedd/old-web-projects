<!DOCTYPE html>
<html>
	<head>
		<title>Mongoose Test App</title>
	</head>
	<body>
		<% if (users) { %>
			<% for (var i in users) { %>
				<p><%= users[i].name %> <%= users[i].age %></p>
			<% } %>
		<% } %>

		<form action="/add_user" method="post">
			<label>Name:</label>
			<input type="text" name="name">
			<label>Age:</label>
			<input type="text" name="age">
			<input type="submit" value="Add">
		</form>
	</body>
</html>